pluginManagement {
  repositories {
    mavenLocal()
    maven {
      name "ave"
      url aveMavenUrl
      credentials {
        username aveMavenUser
        password aveMavenPassword
      }
    }
    gradlePluginPortal()
  }
}

rootProject.name = 'vim-notableft'

def includeBuildsFile = file("includedBuilds.txt")

if (includeBuildsFile.exists()) {
  for (def line : includeBuildsFile.readLines()) {
    // Use # for comments
    if (!line.startsWith('#')) {
      includeBuild line
    }
  }
}

